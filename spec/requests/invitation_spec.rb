require 'rails_helper'

RSpec.describe "Invitations", type: :request do
  # describe 'GET create' do
  #   let!(:invitation) { create :invitation, email: 'test@gmail.com', cycle_id: 3 }

  #   it "opens form modal" do
  #     get :create
  #     expect(response).to have_http_status(:ok)
  #     expect(response.body).to include invitation.email
  #   end
  # end

  # describe "responds to" do
  #   it "responds to html by default" do
  #     post :create, :params => { :invitation => { :email => "test@gmail.com", :cycle_id => 3, :association => cycle } }
  #     expect(response.media_type).to eq "text/html"
  #   end
end
